{
  "type": "object",
  "properties": {

    "editor": {
      "type": "object",
      "description": "preferences for the editor component",

      "properties": {

        "fontFamily": { "type": "string" },
        "fontSize": { "type": ["string", "number"] },
        "fontLigatures": { "type": "boolean", "default": false },

        "rulers": {
          "description": "horizontal rules",
          "type": "array", "items": { "type": "integer" }
        },

        "roundedSelection": {"type": "boolean"},
        "scrollBeyondLastLine": {"type": "boolean"},
        "theme": {
          "description": "one of the built-in theme names (FIXME: custom themes)",
          "type": "string",
          "enum": ["vs", "vs-dark", "hc-black"]
        },
        "lineNumbers": {"type": "boolean"},
        "statusBar": {"type": "boolean"},

        "tabSize": {"type": "integer"},
        "insertSpaces": {"type": "boolean"},

        "minimap": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean", "default": true }
          }
        }
      }
    }
  }
}
